# 연속된 1 만들기

길이 N의 이진 문자열이 주어진다.
문자열은 원형이다. (끝과 처음이 이어져 있음)

문자 '0'을 '1'로 최대 K번까지 바꿀 수 있을 때,
연속된 '1'의 최대 길이를 구하라.

# 입력
`1 ≤ N ≤ 200,000`
`0 ≤ K ≤ N`
`S`는 `'0'`과 `'1'`로만 이루어진 문자열
```
N K
S
```
```
7 1
1101001
```

# 출력

```
연속된 1의 최대 길이
```
```
5
```